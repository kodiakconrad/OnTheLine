# Uncomment the next line to define a global platform for your project
#platform :ios, '9.0'

target 'OnTheLine' do
  use_frameworks!
  # Pods for OnTheLine
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Firestore'
  pod 'FirebaseUI'
  pod 'PureLayout'
  target 'OnTheLineTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OnTheLineUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
